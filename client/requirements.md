## Packages
framer-motion | Smooth animations for dashboard elements
recharts | Visualization of confidence scores and trends
date-fns | Formatting timestamps in the history table
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
